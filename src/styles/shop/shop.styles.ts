// Shops list (superadmin).

// Shop column: the name, then the staff count as one muted line.
export const shopCell = "flex min-w-0 flex-col gap-0.5 whitespace-normal";

export const shopName = "font-medium text-foreground";

export const shopMeta = "text-xs text-muted-foreground";

// Phone rows fold the status under the created date, right-aligned.
export const shopStatusStack = "flex flex-col items-end gap-1";
