// Settings: shop defaults and the signed-in user's account.

export const settingsStack = "flex flex-col gap-4";

export const settingsForm = "flex flex-col gap-4";

export const settingsActions = "flex justify-end";

// Full width on a phone, so the save is one easy thumb target.
export const settingsSubmit = "w-full md:w-auto";

// Manage card: one full-width row per screen under Settings.
export const settingsLinkList = "-mx-2 flex flex-col gap-1";

export const settingsLink = "h-auto min-h-touch w-full justify-start gap-3 px-2 py-2 text-left";

export const settingsLinkText = "flex min-w-0 flex-1 flex-col";

export const settingsLinkLabel = "font-medium text-foreground";

export const settingsLinkHint = "truncate text-xs font-normal text-muted-foreground";

export const settingsLinkChevron = "text-muted-foreground";
