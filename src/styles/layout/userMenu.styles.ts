// Footer row under the nav, on the same side gutter as the header and groups;
// collapsed, it widens like them so the 48px square centres in the rail.
export const userMenuFooter = "px-4 pb-4 group-data-[collapsible=icon]:px-6";

// The registry's lg menu button, held at a 48px square when the rail collapses.
export const userMenuButton =
  "gap-3 px-2 group-data-[collapsible=icon]:size-12! group-data-[collapsible=icon]:justify-center";

export const userMenuText =
  "grid min-w-0 flex-1 text-left leading-tight group-data-[collapsible=icon]:hidden";

export const userMenuName = "truncate text-sm font-semibold text-sidebar-foreground";

export const userMenuMeta = "truncate text-xs text-muted-foreground";

export const userMenuChevron = "ml-auto group-data-[collapsible=icon]:hidden";
