export type ViewLayout = "stack" | "bento";

// "card" frames the whole page shell in one centred card.
export type ViewSurface = "plain" | "card";

export type BentoSpan = "quarter" | "third" | "half" | "twoThirds" | "full";

export type CardTone = "surface" | "ink" | "accent";

export type ModalSize = "sm" | "md" | "lg" | "xl";
